<%= form_for(@user_book) do |f| %>
  <% if @user_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_book.errors.count, "error") %> prohibited this user_book from being saved:</h2>

      <ul>
      <% @user_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br />
    <%= f.text_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :twitter_id %><br />
    <%= f.text_field :twitter_id %>
  </div>
  <div class="field">
    <%= f.label :rental_flag %><br />
    <%= f.text_field :rental_flag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
